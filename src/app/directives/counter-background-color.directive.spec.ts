import { CounterBackgroundColorDirective } from './counter-background-color.directive';

describe('CounterBackgroundColorDirective', () => {
  it('should create an instance', () => {
    const directive = new CounterBackgroundColorDirective();
    expect(directive).toBeTruthy();
  });
});
